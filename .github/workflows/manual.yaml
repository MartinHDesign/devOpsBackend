name: release

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20.14.0"

      - name: Install dependencies
        run: npm install

      - name: Clean dist folder
        run: rm -rf dist/*

      - name: List dist folder
        run: ls -la dist

      - name: Build the project
        run: npm run build

      - name: List dist folder
        run: ls -la dist

      - name: Run tests
        run: npm test

      - name: Create Release
        id: create_release
        if: success() && steps.build.outcome == 'success'
        run: |
          GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}
          gh release create "v1.0.0" dist/*.js --title "Release v1.0.0" --notes "Automated release"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
